import React from "react";
import Button from "ps-react/Button";

/** Custom name, label and color from theme */
export default function ExampleSecondaryButton() {
  return (
    <Button secondary name="myButton" label="Secondary" onClick={() => {}} />
  );
}
