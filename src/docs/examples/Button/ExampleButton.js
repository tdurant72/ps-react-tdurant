import React from "react";
import Button from "ps-react/Button";

/** Custom name, label and color from theme */
export default function ExampleButton() {
  return <Button name="myButton" label="Primary" onClick={() => {}} />;
}
