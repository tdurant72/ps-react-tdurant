import React from "react";
import Button from "ps-react/Button";

/** The big prop increase font size */
export default function ExampleButton() {
    return <Button name="myBigButton" big label="Big" onClick={() => { }} />;
}